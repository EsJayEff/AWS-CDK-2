// import * as cdk from 'aws-cdk-lib';
// import { Template } from 'aws-cdk-lib/assertions';
// import * as Step39AuroraServerlessPostgresWithLambdaLayers from '../lib/step39_aurora_serverless_postgres_with_lambda_layers-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/step39_aurora_serverless_postgres_with_lambda_layers-stack.ts
test('SQS Queue Created', () => {
//   const app = new cdk.App();
//     // WHEN
//   const stack = new Step39AuroraServerlessPostgresWithLambdaLayers.Step39AuroraServerlessPostgresWithLambdaLayersStack(app, 'MyTestStack');
//     // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
